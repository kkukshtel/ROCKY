<?xml version="1.0" encoding="utf-8" ?>
<data>

  <!-- NEED TO MAKE A GLOBAL FONT DEF -->

  <text center_x="true" id="default_desc" x="0" y="150" width="300" text="$GAME_NAME" align="center"/>

  <button id="play" center_x="true" x="0" y="170" label="$TITLE_PLAY">
    <param type="string" value="play"/>
  </button>

  <button id="sound" center_x="true" x="0" y="230" label="$TITLE_SOUND">
    <param type="string" value="sound"/>
  </button>

  <!-- just buttons instead of text buttons? -->

  <!-- Attributes:

x/y, use_def, group
font - string, something like "vera" or "verdana"
size - integer, size of font
style - string, "regular", "bold", "italic", or "bold-italic"
color - hex string, ie, "0xffffff" is white
shadow - shadow color (not working yet maybe?)
align - "left", "center", or "right". Haven't tested "justify"
password_mode - bool, hides text if true -->


</data>
